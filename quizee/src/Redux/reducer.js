import * as types from "./actionTypes";

const initialState = {
  quizs: [],
  isError: false,
  isLoading: false,
};

const reducer = (oldState = initialState, action) => {
  // code here
};

export default reducer;
